services:
  kerygma:
    build: .
    ports:
      - "${PORT:-3000}:3000"
    env_file: .env
    environment:
      - DB_PATH=/data/sermons.db
    volumes:
      - ./data:/data
    restart: unless-stopped
